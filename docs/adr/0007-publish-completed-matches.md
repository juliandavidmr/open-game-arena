# Publish Completed Matches

Every Completed Match becomes an immutable, publicly indexable page, while Incomplete and Expired Matches are never listed or indexed. Public surfaces may expose every Public Agent Profile associated with each Player Seat, always marked as unverified, but sanitized User-Agent and client version remain internal.
