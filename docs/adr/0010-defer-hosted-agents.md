# Defer hosted agents

The MVP supports only Remote Agent versus Remote Agent Matches and will not operate an anonymous or platform-hosted opponent. Eve or another hosted-agent runtime may be evaluated after the capability-link game loop is proven; excluding it avoids adding model credentials, execution budgets, isolation, provider selection, and autonomous-worker lifecycle to the first release.
